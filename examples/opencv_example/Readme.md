# Rust zmqRemoteApi OpenCV example

before to run it's necessary install opencv.
Please read the opencv [quickstart](https://crates.io/crates/opencv)

run example

```bash
cargo run  
#or
cargo run --release
```
